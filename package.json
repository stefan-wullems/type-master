{
  "name": "type-master-chrome-plugin",
  "private": true,
  "scripts": {
    "build": "esbuild src/content.ts src/background.ts --bundle --outdir=dist --format=iife",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0",
    "jsdom": "^25.0.0"
  }
}
